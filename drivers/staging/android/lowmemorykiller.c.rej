diff a/drivers/staging/android/lowmemorykiller.c b/drivers/staging/android/lowmemorykiller.c	(rejected hunks)
@@ -243,7 +243,7 @@ static int lowmem_shrink(struct shrinker *s, struct shrink_control *sc)
 		     sc->nr_to_scan, sc->gfp_mask, rem);
 	read_unlock(&tasklist_lock);
     if (selected)
-        compact_nodes();
+        compact_nodes(false);
 	return rem;
 }
 
